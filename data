---
class: standard
completed: 2026-08-09
created: 2026-08-09T19:33:38Z
id: 83
priority: high
status: done
tags:
- upstream
- libgit2
- release
- tracker
title: 'track: release p5-git-libgit2 with git_reference_create_matching, then bump
  karr cpanfile pin'
updated: 2026-08-09T20:50:26Z
---

Upstream situation as of 2026-08-09:

- p5-git-libgit2 commit bcc61de "Bind matching reference creation" landed on main. git_reference_create_matching is now bound (lib/Git/Libgit2/FFI.pm:144 + 719-721). Tests added in t/10-reference.t.
- p5-git-native commit ff219cd (this session) added Repository::reference_create expected_old => and Repository::reference_set_target, both backed by git_reference_create_matching. The create-CAS subtest in t/50-reference-cas.t was previously skipping because the binding was missing — it should now run.
- Neither repo has been released. Installed locally: Git::Libgit2 0.005 / Git::Native 0.004. CPAN: 0.004 / 0.003.

Work this ticket tracks (do NOT release — that is the maintainers call):

1. Maintainer: bump Git::Libgit2 to 0.006, run dzil release.
2. Maintainer: bump Git::Native to 0.005 and update cpanfile to depend on Git::Libgit2 0.006 (perl-core rule: never copy the repo version; use cpanm --info to discover the released version). Run dzil release.
3. After both releases: bump karr cpanfile pin to Git::Native >= 0.005 / Git::Libgit2 >= 0.006.
4. karr migrate write_ref in the Lock::acquire path (#46) and the allocate_next_id path (#44) to use Repository::reference_create(expected_old => $old) and Repository::reference_set_target(expected_old => $old) instead of the current force-overwrite. On is_not_matched, retry.

Not blocking: the permission spec quirk in Git::Native::Remote::Result::ok() — flagged in the upstream handoff note, not a blocker for karr (karr only cares about rejected, not ok).
Steps 1-3 done.

Released: Git::Libgit2 0.005 (2026-08-09 20:03Z, with
git_reference_create_matching) and Git::Native 0.004 (2026-08-09 20:36Z, with
expected_old => on reference_create/reference_set_target plus the per-ref
fetch/push Result). Note the numbers came out one lower than this ticket
predicted: the maintainer released the repo versions as they stood rather than
bumping first, so the released pair is 0.005 / 0.004, not 0.006 / 0.005.

karr cpanfile now pins Git::Native 0.004 / Git::Libgit2 0.005 (verified against
cpanm --info and MetaCPAN, not the repo $VERSIONs, which are 0.006 / 0.005 and
unreleased).

Step 4 — migrating write_ref, Lock::acquire and allocate_next_id to the CAS API
with a retry on is_not_matched — is karr-side work and lives in #46 and #44.
The upstream tickets are #80 (done) and #81 (done upstream, karr side split to
#84).
